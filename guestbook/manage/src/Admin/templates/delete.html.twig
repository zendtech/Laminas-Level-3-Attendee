{% extends '@layout/default.html.twig' %}

{% block title %}Delete{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-md-12">
            <h2>Delete Guestbook Listing</h2>
            <hr>
            <table style="width:100%;">
            <tr><th>ID</th><th>Name</th><th>Email</th><th>&nbsp;</th></tr>
            <tr>
            <td>{{ entry.id }}</td>
            <td>{{ entry.name }}</td>
            <td>{{ entry.email }}</td>
            <td><a href="{{ path('manage.confirm',{ 'id' : entry.id }) }}?confirm=1">CONFIRM</a></td>
            </tr>
            </table>
        </div>
    </div>
{% endblock %}
